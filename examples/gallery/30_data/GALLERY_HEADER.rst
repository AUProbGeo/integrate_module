Data
----

Joint inversion is supported by using multiple data types. The first example
splits a single tTEM data set (with both low and high moment) into two separate
data sets, which can then be inverted separately or jointly. Borehole data can
also be used, either on its own or in combination with tTEM data.
